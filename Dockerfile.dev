# ── Dev Dockerfile — Vite HMR with hot reload ──────────
FROM node:20-alpine

WORKDIR /app

# Install deps first (cached layer)
COPY package.json package-lock.json ./
RUN npm ci

# Source mounted as volume — not COPY'd
EXPOSE 5173

CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
